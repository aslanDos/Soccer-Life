import 'package:flutter/material.dart';

class FavoritesGamesTab extends StatelessWidget {
  const FavoritesGamesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Favorites Games Tab'));
  }
}
