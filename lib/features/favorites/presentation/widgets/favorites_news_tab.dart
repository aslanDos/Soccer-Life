import 'package:flutter/material.dart';

class FavoritesNewsTab extends StatelessWidget {
  const FavoritesNewsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Favorites News Tab'));
  }
}
