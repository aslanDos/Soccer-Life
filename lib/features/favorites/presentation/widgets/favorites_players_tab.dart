import 'package:flutter/material.dart';

class FavoritesPlayersTab extends StatelessWidget {
  const FavoritesPlayersTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Favorites Players Tab'));
  }
}
