import 'package:flutter/material.dart';

class LeagueOverviewTab extends StatelessWidget {
  const LeagueOverviewTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Overview'));
  }
}
